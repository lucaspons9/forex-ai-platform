[core]
# The home folder for airflow, default is ~/airflow
airflow_home = /app/airflow

# The folder where your airflow pipelines live, most likely a
# subfolder in a code repository
dags_folder = /app/airflow/dags

# The folder where airflow should store its log files
base_log_folder = /app/airflow/logs

# The executor class that airflow should use. Choices include
# SequentialExecutor, LocalExecutor, CeleryExecutor
executor = LocalExecutor

# The SqlAlchemy connection string to the metadata database
sql_alchemy_conn = postgresql+psycopg2://user:password@db/airflow

# The URL to the airflow UI
web_server_url = http://localhost:8080

[webserver]
# The base url of your web application
base_url = http://localhost:8080

# The ip specified when starting the web server
web_server_host = 0.0.0.0

# The port on which to run the web server
web_server_port = 8080

# The amount of time (in seconds) the gunicorn webserver waits before timing out on a worker
web_server_worker_timeout = 120
